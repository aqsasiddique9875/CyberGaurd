streamlit>=1.20.0
numpy
librosa
matplotlib
soundfile
pillow
requests
pandas
scipy
audioread
# Optional: Whisper (very large, requires torch). Install only if you need transcription on-device.
# Either use the PyPI package (if available) or install from GitHub:
# openai-whisper
# or
# git+https://github.com/openai/whisper.git
# If you want Whisper + PyTorch add:
# torch >=1.12.0
